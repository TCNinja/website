{
  "name": "json-server",
  "version": "1.0.0",
  "description": "JSON server for tcninja",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "json-server --watch db.json --port 3004"
  },
  "author": "Ori Daniel",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.0"
  }
}
